pipeline {
    agent any

    stages {
        stage('Checkout_Code') {
            steps {
                sh 'git credentialsId: 'github_id', url: 'https://github.com/Muzammil-2/java-web-app-docker.git''
            }
        }
    }
}